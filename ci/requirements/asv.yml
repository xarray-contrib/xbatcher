name: xbatcher-benchmarks
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Required dependencies
  - python=3.10
  - dask
  - numpy
  - xarray
  - pytorch
  - tensorflow
  # Xbatcher installation
  - pip
